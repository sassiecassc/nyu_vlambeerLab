using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class waveGen : MonoBehaviour
{
    /*
    public GameObject wave;
    public int waveCounterMax = 1000;

    // Start is called before the first frame update
    void Start()
    {
        int waveCounter = 0;

        while(waveCounter < waveCounterMax)
        {

            Vector2 spawnPosition = new Vector2(Random.Range(-40f, 40f), Random.Range(-40f, 40f));

            //GameObject newWave = Instantiate(wave, spawnPosition, Quaternion.Euler())
        }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    */
}
